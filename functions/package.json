{
  "name": "board-game-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.3.0",
    "firebase-functions": "^6.3.2",
    "pokersolver": "^2.1.4"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "@types/node": "^20.0.0"
  },
  "private": true
}
